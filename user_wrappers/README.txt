Drop trusted wrapper files here, or register them through the UI.

Each wrapper should inherit from BaseScanAdapter and implement the methods it actually supports.

The platform treats this as a trusted-admin plugin surface, not an untrusted public upload mechanism.
